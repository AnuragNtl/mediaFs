#ifndef UTILS_H

#define UTILS_H

#include <string>
#include <vector>

#define LEN_SEPARATOR ","

namespace MediaFs {

    std::vector<std::string> split(std::string, std::string delim = " ");
};

#endif

